DATABASE_URL="file:./dev.db"

# JWT Secret for authentication - in production, use a strong random string
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="development-secret-key-change-in-production"

# Admin key for protected routes - in production, use a strong random string
# Generate using: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ADMIN_KEY="secure-admin-key"

# Secret for protecting cron job endpoints
CRON_SECRET="cron-secret-key"

# Base URL for the application
NEXT_PUBLIC_APP_URL="http://localhost:3000"